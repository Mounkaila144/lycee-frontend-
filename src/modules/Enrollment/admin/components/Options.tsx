'use client';

import { OptionList } from './OptionList';

/**
 * Options Page Component
 * Admin interface for managing student option choices
 */
export const Options = () => {
  return <OptionList />;
};

export default Options;
