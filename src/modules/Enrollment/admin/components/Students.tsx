'use client';

import { StudentList } from './StudentList';

/**
 * Students Page Component
 * Main page for student management with list and CRUD operations
 */
export const Students = () => {
  return <StudentList />;
};
